import assert from 'node:assert/strict'
import { readFile } from 'node:fs/promises'

const model = await readFile(new URL('../src/domain/construction/constructionModel.ts', import.meta.url), 'utf8')
const topology = await readFile(new URL('../src/domain/construction/fieldTopology.ts', import.meta.url), 'utf8')
const shell = await readFile(new URL('../src/components/ConstructorShell.tsx', import.meta.url), 'utf8')
const shellCss = await readFile(new URL('../src/components/ConstructorShell.css', import.meta.url), 'utf8')
const offerModules = await readFile(new URL('../src/domain/offerModules.ts', import.meta.url), 'utf8')
const app = await readFile(new URL('../src/App.tsx', import.meta.url), 'utf8')
const acceptance = await readFile(new URL('../docs/CONSTRUCTOR_01D_FIELD_TYPES_VISUAL_OPENING_ACCEPTANCE.md', import.meta.url), 'utf8')
const packageJson = JSON.parse(await readFile(new URL('../package.json', import.meta.url), 'utf8'))

assert.match(model, /field-topology-07/)
assert.match(model, /constructor-01d/)
assert.match(model, /ConstructionFieldType = 'fixed' \| 'operable'/)
assert.match(topology, /setConstructionFieldType/)
assert.match(topology, /setConstructionFieldOpeningMode/)
assert.match(topology, /setConstructionFieldOpeningHanding/)
assert.match(topology, /openingMode === 'side-hinged' \|\| openingMode === 'tilt-turn'/)
assert.match(shell, /FIELD SEMANTICS 01D/)
assert.match(shell, /activeTool === 'fixed-field'/)
assert.match(shell, /activeTool === 'operable-field'/)
assert.match(shell, /Отваряемо \/ крило/)
assert.match(shell, /constructor-operable-visual/)
assert.match(shell, /Странично \+ падащо/)
assert.match(shellCss, /constructor-operable-visual/)
assert.match(offerModules, /fieldTypeSource: constructorFieldType \? 'constructor' : 'unset'/)
assert.match(offerModules, /openingModeSource: constructorOpeningMode \? 'constructor' : 'unset'/)
assert.match(app, /fieldType: field\.fieldType/)
assert.match(app, /openingMode: field\.openingMode/)
assert.match(app, /openingHanding: field\.openingHanding/)
assert.match(app, /Типът на ПОЛЕТО и отваряемостта идват от Конструктора/)
assert.match(acceptance, /OPERABLE VISUAL SLOT: CANONICAL/)
assert.match(acceptance, /WORKING OPENING SYMBOLS: DEFINED IN 01D\.1/)
assert.match(acceptance, /PROFILE RESOLUTION: NO/)
assert.match(acceptance, /MACHINE READY: NO/)
assert.match(packageJson.scripts['test:contract'], /verify-constructor01d\.mjs/)

console.log('CONSTRUCTOR 01D FIELD TYPES + VISUAL OPENING VERIFY PASS')
console.log('FIELD TYPES: FIXED | OPERABLE | UNSET')
console.log('OPERABLE SASH: LOGICAL | VISUAL SLOT CANONICAL')
console.log('OPENING MODE: SIDE-HINGED | TILT | TILT-TURN')
console.log('LEFT/RIGHT: HUMAN WORKING HANDING ONLY')
console.log('WORKING OPENING SYMBOLS: DEFINED IN 01D.1')
console.log('PROFILE RESOLUTION: NO')
console.log('MACHINE READY: NO')
