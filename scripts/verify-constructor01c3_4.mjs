import assert from 'node:assert/strict'
import { readFile } from 'node:fs/promises'

const app = await readFile(new URL('../src/App.tsx', import.meta.url), 'utf8')
const shell = await readFile(new URL('../src/components/ConstructorShell.tsx', import.meta.url), 'utf8')
const css = await readFile(new URL('../src/components/ConstructorShell.css', import.meta.url), 'utf8')
const acceptance = await readFile(new URL('../docs/CONSTRUCTOR_01C_3_4_FREE_SKETCH_MODULE_WORKSPACE_ACCEPTANCE.md', import.meta.url), 'utf8')
const packageJson = JSON.parse(await readFile(new URL('../package.json', import.meta.url), 'utf8'))

assert.match(app, /type FreeConstructorModule/)
assert.match(app, /freeModules/)
assert.match(app, /activeFreeModuleId/)
assert.match(app, /freeModuleSketchDrafts/)
assert.match(app, /createNextFreeModule/)
assert.match(app, /selectFreeModule/)
assert.match(app, /setActiveFreeModuleDraft/)
assert.match(app, /resetActiveFreeModuleDraft/)
assert.match(app, /key=\{activeFreeModule\?\.id \?\? 'free-no-module'\}/)
assert.match(app, /moduleItems=\{freeModules\.map/)
assert.match(app, /onCreateModule=\{createNextFreeModule\}/)

assert.match(shell, /(?:FIELD TOPOLOGY 01C\.3\.[4567]|FIELD SEMANTICS 01D)/)
assert.match(shell, /hasActiveModule/)
assert.match(shell, /canEditConstruction/)
assert.match(shell, /showModuleStrip/)
assert.match(shell, /\+ Създай Модул 1/)
assert.match(shell, /\+ Нов модул/)
assert.match(shell, /Изчисти Модул/)
assert.match(shell, /constructor-module-start-hint/)
assert.match(shell, /Свободна скица · без модул/)
assert.match(shell, /Свободна скица · Модул/)

assert.match(css, /\.constructor-module-empty/)
assert.match(css, /\.constructor-create-first-module/)
assert.match(css, /\.constructor-module-start-hint/)

assert.match(acceptance, /Create Module 1/i)
assert.match(acceptance, /own independent Constructor draft/i)
assert.match(acceptance, /switch back/i)
assert.match(acceptance, /only the active module/i)
assert.match(acceptance, /ANGLED DIVIDER: NEXT STAGE/)
assert.match(acceptance, /PROFILE RESOLUTION: NO/)
assert.match(acceptance, /MACHINE READY: NO/)
assert.match(packageJson.scripts['test:contract'], /verify-constructor01c3_4\.mjs/)

console.log('CONSTRUCTOR 01C.3.4 FREE SKETCH MODULE WORKSPACE VERIFY PASS')
console.log('FREE CONSTRUCTOR: CREATE MODULE 1 -> NEW MODULE -> SWITCH MODULES')
console.log('PER-MODULE DRAFT: YES | RESET ACTIVE MODULE ONLY')
console.log('DRAWING WITHOUT MODULE: BLOCKED')
console.log('ANGLED DIVIDER: NEXT STAGE')
console.log('PROFILE RESOLUTION: NO')
console.log('MACHINE READY: NO')
